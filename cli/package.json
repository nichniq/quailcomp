{
  "name": "@quailcomp/cli",
  "version": "0.1.0",
  "description": "Command-line interface for Quailcomp personal data management",
  "type": "module",
  "bin": {
    "quailcomp": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "dev": "bun run src/index.ts",
    "cli": "bun run src/index.ts"
  },
  "dependencies": {
    "@quailcomp/data": "workspace:*",
    "@quailcomp/book-metadata": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6"
  }
}
