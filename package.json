{
  "name": "quailcomp",
  "type": "module",
  "license": "Unlicense",
  "workspaces": ["data/client", "server", "frontend", "services/book-metadata", "cli"],
  "scripts": {
    "test": "bun run lint && bun test --cwd data/client && bun test --cwd server && bun test --cwd cli",
    "test:watch": "bun test --watch --cwd data/client",
    "test:unit": "bun test --cwd data/client && bun test --cwd server && bun test --cwd cli",
    "test:coverage": "bun run test:coverage:generate && bun run test:coverage:report",
    "test:coverage:generate": "bun run test:coverage:clean && bun run test:coverage:workspaces && bun run test:coverage:root && bun run test:coverage:merge",
    "test:coverage:workspaces": "bun test --coverage --coverage-reporter=lcov --cwd data/client && bun test --coverage --coverage-reporter=lcov --cwd cli && bun test --coverage --coverage-reporter=lcov --cwd services/book-metadata",
    "test:coverage:root": "bun test --coverage --coverage-reporter=lcov misc.test.ts",
    "test:coverage:merge": "bun run scripts/merge-coverage.ts",
    "test:coverage:report": "bun run scripts/coverage-report.ts",
    "test:coverage:clean": "rm -rf coverage data/client/coverage server/coverage cli/coverage services/book-metadata/coverage",
    "type-check": "bun run scripts/type-check.ts",
    "db:setup": "bun run --cwd data/client setup",
    "db:teardown": "bun run --cwd data/client teardown",
    "db:migrate": "bun run --cwd data/client migrate",
    "lint": "eslint . --max-warnings=0 && markdownlint-cli2",
    "lint:fix": "eslint . --fix --max-warnings=0 && markdownlint-cli2 --fix",
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix",
    "install-hooks": "bash scripts/install-hooks.sh",
    "cli": "bun run --cwd cli cli"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "bun-types": "^1.3.7",
    "eslint": "^9.39.2",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-vue": "^10.7.0",
    "markdownlint-cli2": "^0.20.0",
    "vue-eslint-parser": "^10.2.0"
  }
}
